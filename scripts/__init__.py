"""Runnable CLI scripts for the HAM10000 project.

Each script is a self-contained ``python -m scripts.<name>`` entry point
that uses the reusable functions from :mod:`src`. They are kept thin on
purpose — orchestration only, no analysis logic.
"""
